/**
 * 首次登录（没有集团权限）
 * @version  v1.0
 * @createTime: 2016/5/25 
 * @createAuthor zuoxh
 * @updateHistory
 */
define(["rippleButton"], function (rippleButton) {
    // var ripple = require("rippleButton");
    return ["$scope", "$rootScope", "register",function ($scope, $rootScope, register) {
    	$rootScope.overlay = true;

    }];
});